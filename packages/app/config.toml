# Frontend configuration for OpenCode web UI
# Environment: selected via VITE_APP_ENV (default: "dev")
# Override individual values with VITE_OPENCODE_SERVER_* env vars

[default.server]
host = "localhost"
port = 4097

[dev.server]
# Uses default values

[test.server]
# Uses default values

[production.server]
host = "0.0.0.0"
port = 443
